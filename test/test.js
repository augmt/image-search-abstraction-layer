'use strict';

describe('app', function () {
  describe('when searching for images', function () {
    it('should respond with json');

    it('should return specific metadata about each image');

    it('should allow pagination');
  });

  describe('when retrieving recent searches', function () {
    it('should respond with json');

    it('should display all the recently submitted search strings');
  });
});
